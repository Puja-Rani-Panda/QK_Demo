name: puja

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
  
    - name: Build the site in the jekyll/builder container
      run: echo "Hello World"
        
